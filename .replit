run = "python main.py"
modules = ["python-3.12", "nix"]

# 部署配置
[deployment]
build = "pip install -e ."

# 自动重启
[debugger]
enabled = true

[nix]
channel = "stable-25_05"

# 环境变量文件 (不会被提交到代码仓库)
# Secrets 应该在 Replit 的Secrets面板中配置
